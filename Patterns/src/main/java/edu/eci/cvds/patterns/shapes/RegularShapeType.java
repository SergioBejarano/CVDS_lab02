package edu.eci.cvds.patterns.shapes;

/**
 * Enum representing different types of regular forms.
 * Each of this enum corresponds to a specific regular shape.
 */
public enum RegularShapeType {
    Triangle, Quadrilateral, Pentagon, Hexagon
}